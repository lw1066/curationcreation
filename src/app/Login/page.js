"use client";
import Login from "../components/Login";

const LoginPage = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        flexDirection: "column",
      }}
    >
      <Login />
    </div>
  );
};

export default LoginPage;
